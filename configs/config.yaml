server:
  port: 8080
  mode: "debug" # debug для разработки, release для продакшена

database:
  host: "localhost"
  port: 5432
  user: "root"
  password: "secretpassword"
  name: "bimpro_db"
  sslmode: "disable"